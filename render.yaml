services:
  - type: web
    name: civilis-risk-os-runtime
    env: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm run runtime:hosted
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
